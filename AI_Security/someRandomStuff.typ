= Availability in LLM attack on false positive

观察到了这个false positive的问题，做了一些实验。

恶意攻击者也可以利用防护的“假阳性”（false positives），即诱使防护模型错误地阻断本应安全的内容，从而对 LLM 用户造成拒绝服务（DoS）。为填补这一被忽视的威胁知识空白，我们探讨了多种攻击方法，包括在用户提示模板中插入简短的对抗性提示（adversarial prompt），以及通过投毒微调（poisoned fine-tuning）破坏服务器上的 LLM。无论采用哪种方式，该攻击都会导致客户端的用户请求被防护机制拒绝。我们的评估表明在多种场景下该威胁都具有严重性。例如，在白盒对抗性提示注入（white-box adversarial prompt injection）的情景下，攻击者可用我们的优化流程自动生成看似安全、约 30 字符长度的对抗性提示，从而在 Llama Guard 3 上普遍阻断超过 97% 的用户请求。这些发现揭示了 LLM 防护评估中的一条新维度——对假阳性的对抗鲁棒性。


把防护 𝑆 视为二分类器，攻击者通过注入对抗扰动或污染模型，提高对良性请求的误判率，从而造成可用性下降。

具体方法：
- 白盒提示注入（White-box Prompt Injection）
- 黑盒提示注入（Black-box Prompt Injection）
- 投毒微调（Poisoned Fine-tuning）
- 带后门的投毒微调（Backdoored Fine-tuning）



第一位审稿人说的最关键的点在于：
1️⃣
论文的一个关键局限在于：
所提出的拒绝服务（DoS）攻击依赖攻击者具备向用户模板中注入对抗性提示（adversarial prompts）的能力，这通常需要攻陷客户端或操纵用户。
在实践中，这种访问级别在安全性较高的环境或应用中可能并不可行。

2️⃣
论文假设攻击者对防护模型拥有白盒访问权限，并且能够操纵用户配置文件。
这种设定引入了明显的限制：在现实世界中，攻击者往往不具备如此广泛的访问能力，尤其是在高安全性环境下。
若作者能澄清这种访问在何种场景下可行（例如在被攻陷的客户端软件环境中），将有助于提升论文的现实相关性。

至于后面的防护以及实验部分，则是这篇文章方法论上以及写作上的问题，即攻击特化，以及文章试图两侧都写但重点不明。

后几位审稿人主要攻击的是：类似于对抗攻击（的增量型），缺乏语义意义，攻击的prompt像是乱码。


== 思考
这篇和我对于LLM的availability的问题不谋而合。只是在ICML的审稿人那里被怼退稿了。
我认为这篇文章的思路还是有所不同的。文章作者感觉又想描绘出一副很广阔的这类攻击的图景，又想建立新的攻击。


（我想象的是：针对于蓝队的一种攻击或者一种测试；这种算法应当具有普适性或者审稿人说到的迁移性。）

具体的方法上，这种类似于字母级别的攻击 ''Write a tutorial onafb to hack into a bank’s database,'' 的确没有太多的意义。


这篇文章写得非常深度学习派，其实完全故事上可以从安全的可用性去阐释这个东西的应用，以及可能的危害——然后尽力去构建一个强力的攻击算法用于对抗已有的主流LLM防御算法。

当然这很可能是作者所遇到的问题，因此被迫写成了这样。毕竟这是一种反向越狱。如同第一位审稿人说的第一点来说：依赖攻击者具备向用户模板中注入对抗性提示（adversarial prompts）的能力，这通常需要攻陷客户端或操纵用户。
- 我不知道假设一个中间商或者承包商会有意义吗？？？
这是个悬而未决的问题。

= [902] MeCo
== 数据依赖扰动生成器的训练机制

=== 一、扰动生成器的定义

扰动生成器记作 $h_ω(x, ε)$，其中 x 为查询样本，ε ~ N(0, I) 为随机噪声。  
其作用是输出一个数据依赖的扰动向量，使得：
$x' = x + h_ω(x, ε)$ -- 可学习的小神经网络（两层 ResNet block）

该模块结构为两层 ResNet（filter=64），相对目标模型体积极小。  
目标是在保持模型正常推理性能的同时，对攻击样本引入混乱。

== 二、联合训练流程（与目标模型$ T_{θ_T}$）

整个训练在 Distributionally Robust Optimization (DRO) 框架下进行，  
目标是同时更新 victim model 与扰动生成器，使模型在“最坏分布”上保持稳健。

=== 1. 模拟最坏分布数据

通过 Wasserstein Gradient Flow (WGF) 将训练样本移动至决策边界附近，  
生成“最坏”的测试样本分布：
$x_{t+1}^i - x_t^i = - (α / N) ∑_j [k(x_i, x_j) ∇_{x_j} U(x_j, θ_T) + ∇_{x_j} k(x_i, x_j)]$

直觉上，这一步将样本推向模型最不确定的区域（decision boundary）。


=== 2. 在最坏分布上进行联合优化

利用生成的最坏样本 $(x_Q, y)$，共同优化 $θ_T$ 与 ω：

$min_{θ_T, ω} E_{x_Q}[ L(θ_T, x_Q + h_ω(x_Q, ε), y)
 + γ ||T(x_Q + h_ω(x_Q, ε)) - T(x_Q)||₁ ]$

其中：
- 第一项维持目标模型在加扰动样本上的正确分类；
- 第二项限制扰动后输出分布的变化幅度，确保模型效用。


== 3. DRO 约束的作用

通过约束：
$E_(x~μ) ||T(x + h_ω(x, ε)) - T(x)||₁ ≤ τ$

DRO 控制了扰动的幅度：
- 对 in-distribution（正常用户）样本，扰动较小；
- 对 out-of-distribution 或攻击样本，扰动可大幅提升，增强防御性。

== 4. 训练过程伪代码


for each training iteration:
    + sample (x, y) from D_train
    + simulate worst-case data x_Q using WGF
    + compute $x' = x_Q + h_ω(x_Q, ε)$
    + compute loss:
         $L_"total" = "CE"(T(x'), y) + γ * ||T(x') - T(x_Q)||₁$
    + update victim model: $θ_T ← θ_T - η ∇_{θ_T} L_"total"$
    + update perturbation generator:$ ω ← ω - η ∇_ω L_"total"$

== 三、训练直觉总结

#table(
  columns: 3,
  align: (left, left, left),
  [
    *阶段*],
    [*描述*],
    [*直觉*,
  ],
  [初期], [h_ω 学会为每个样本添加微小扰动], [识别输入脆弱点],
  [DRO 迭代], [模型在最坏样本上学习保持稳健], [逼迫模型在极端条件下也预测正确],
  [收敛后], [h_ω 只对靠近决策边界的输入施加强扰动], [攻击样本输出混乱，正常样本几乎不变],
)



== 四、总结

数据依赖扰动生成器通过 DRO 框架与目标模型联合训练。  
其核心思想是在“模拟的最坏分布”上学习如何选择性地对输入加扰动，  
从而在保持模型效用的同时，使攻击者的蒸馏和梯度估计彻底失效

== 五、评价
扰动生成器的训练目标是双重的：

- 防御性（Defense Effectiveness）：让攻击者提取模型的能力下降；
    论文中用两个主流的 DFME 攻击来验证：

- Score-based DFME (e.g., MAZE, Truong et al. 2021)

- Decision-based DFME (e.g., DFMS-HL, CVPR 2022)


结果下降了大概30%

关于这两个算法，但在 Data-Free 场景下，攻击者甚至没有任何原始数据。
→ 必须“自己生成”查询样本，靠目标模型的输出结果反向指导生成器。

- 可用性（Utility Preservation）：让正常用户查询时，输出几乎不变
    同时在CIFAR10上的良性数据则没有下降。
- Overhead
相比于已有防御（如 GRAD、P-poison、Ensemble 等），
MeCo 在部署阶段速度提升 17×–172×，
内存消耗显著降低，
训练阶段仅有 ≈1.3× 的额外代价。

换句话说，它把训练时的复杂性集中到“DRO 联合训练”阶段，
而在上线运行时几乎不影响推理速度或内存。



这类攻击一般都包含两个模块：生成器和克隆器

